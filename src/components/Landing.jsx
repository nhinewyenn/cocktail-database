import React from 'react';
import Nav from './Nav';
import SearchForm from './SearchForm';

const Landing = () => {
  return (
    <section id="landing">
      <Nav />
      <SearchForm />
    </section>
  );
};

export default Landing;
